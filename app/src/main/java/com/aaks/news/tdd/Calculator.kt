package com.aaks.news.tdd

class Calculator {

    fun add(firstValue: Int, secondValue: Int) : Int{
        return firstValue + secondValue;
    }

    fun add(firstValue: Int, secondValue: Int, thirdValue: Int) : Int{
        return firstValue + secondValue + thirdValue;
    }
}